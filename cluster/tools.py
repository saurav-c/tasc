#!/usr/bin/env python3

import os
import sys
from add_nodes import add_nodes
import util

NODE_TYPES = ['tasc', 'keynode', 'routing', 'lb', 'worker']

def main():
    args = sys.argv

if __name__ == '__main__':
    main()