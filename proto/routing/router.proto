syntax="proto3";

service Router{
    rpc LookUp(RouterReq) returns (RouterResponse) {}
}

message RouterReq {
    string req = 1;
}

message RouterResponse {
    string ip = 1;
    RouterError error = 2;
}

enum RouterError {
    SUCCESS = 0;
    FAILURE = 1;
}

